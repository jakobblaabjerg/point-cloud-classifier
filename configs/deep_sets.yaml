model:
  input_dim: 5
  phi_layers: [64, 64, 64]
  rho_layers: [64, 64, 64]
  output_dim: 1
  sparse_batching: true
  pooling: "mean"

dataset:
  batch_size: 32
  sparse_batching: true

trainer:
  epochs: 8
  learning_rate: 0.001 
